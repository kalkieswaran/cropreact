import { CROP_LOGO_LIGHT } from "../../services/ImageService";

const SideNav = ({  }) => {
  return (
    <div>
        <div>
            <img src={CROP_LOGO_LIGHT} />
        </div>      
    </div>
  );
};

export default SideNav;
